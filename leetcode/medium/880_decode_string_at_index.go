package medium

import ()

func DecodeStringAtIndex(S string, K int) {
	// Brutal approach: print them all!

	// Methamatical approach

	strs := []string{"leet", "code"}
	//is := []int{2, 3}

	currentIndex := 0
	for i := 0; i < len(strs); i++ {
		for currentIndex < K {
		}

	}
}
